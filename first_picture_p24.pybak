def first_picture():
 my_file=pickAFile()#this gets a string that points to a file location
 print my_file #prives that the pickAFile() just gets a text string
 my_picture=makePicture(my_file) #makes the jpg viewable formatting array
 explore(my_picture) #could use show(my_picture)
def shorter_version():
 my_picture=makePicture(pickAFile())
 explore(my_picture)