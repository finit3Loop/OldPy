def firstFor(entString):
  for i in entString: #naming an indicater variable, creates variable called i, creates list of the input,for loops always have lists,i changes from first to last in order
    print i , # need an indent, prints the list assigned to i, tabbing builds the structure, goes through instructions and applies them to the list items one at a time
    ordi=ord(i)
    print ordi,
    print ordi*838,
def secondFor():
  for i in "Jarret Piper": #set list in the function
    print i,
#Lists (index) are typed automatically as text, index is number 01234.... starts on 0