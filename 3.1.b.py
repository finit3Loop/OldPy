def sillyString():
  print """We can use many quotes
    because then we can run
    multiple lines"""
  print " Hey I want a \" single double tick there" #looks for pairs of " or '
  print "Here it's a single tic inside doubles"
  print 'Here its a single tic pair' #won't work with a 3rd ' without escape before
  print 'Here it\'s a single tic'
  print "Sean Says, \"Watch 'yer ticks\"" #to see the " you have to escape before